<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.asopoketool.mapper.MatchResultMapper">

    <select id="findByMatchId" resultType="com.asopoketool.model.MatchResult">
        SELECT * FROM match_result WHERE match_id = #{matchId}
    </select>

    <insert id="insert">
        INSERT INTO match_result (match_id, winner_entry_id, registered_by, registered_at)
        VALUES (#{matchId}, #{winnerEntryId}, #{registeredBy}, NOW())
    </insert>

    <update id="update">
        UPDATE match_result 
        SET winner_entry_id = #{winnerEntryId}, registered_by = #{registeredBy}, registered_at = NOW() 
        WHERE match_id = #{matchId}
    </update>

</mapper>
